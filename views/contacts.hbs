<!DOCTYPE html>
<html>
<head>
    <title>Steven</title>
    {{>globalScript}}
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <script src="https://use.fontawesome.com/f4dc2fb042.js"></script>

    <style>
        /*Contact*/
        .contactContainer{
            display:table;
            width: 100%;
        }
        .contactContainer .contactIconContainer{
            display:table-cell;
            text-align: center;
            vertical-align: middle;
        }
        .contactContainer .contactIcon{
            border-radius:50%;
            border: 1.4px solid black;
            margin: 0 auto;
            height: 8em;
            width: 8em;
        }
        .contactContainer .contactIcon i{
            display:block;
            height: 100%;
            position: relative;
            cursor: pointer;
        }
        .contactContainer .contactIcon i::before{
            position: absolute;
            top: 50%;
            left: 50%;
            -webkit-transform: translate(-50%,-50%);
            -ms-transform: translate(-50%,-50%);
            transform: translate(-50%,-50%);
            font-size: 60px;
        }
        .contactContainer .contactIcon i:hover:before{
            color:white;
        }
        .contactContainer .contactIcon i::after{
            -webkit-transform: scale(1.3);
            -moz-transform: scale(1.3);
            -ms-transform: scale(1.3);
            transform: scale(1.3);
            opacity: 0;

            top: 0px;
            left: 0px;
            padding: 2px;
            z-index: -1;
            background: black;
            -webkit-transition: -webkit-transform 0.2s, opacity 0.3s;
            -moz-transition: -moz-transform 0.2s, opacity 0.3s;
            transition: transform 0.2s, opacity 0.3s;

            pointer-events: none;
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            content: '';
        }
        .contactContainer .contactIcon i:hover:after{
            opacity: 1;
            transform: scale(1);
        }
    </style>

</head>
<body>
  <div class="wrapper">
        {{>navigationBar}}
        <div id="content">
            <div class="jumbotron jumbotron-fluid">
                <div class="container">
                    <h1>{{pageTitle}}</h1>
                </div>
            </div>
            {{!-- {{{contents}}} --}}
            <div class="contactContainer">
                <div class="contactIconContainer">
                    <div class="contactIcon">
                        <a target="_blank" href="https://www.linkedin.com/in/steven-steven1999/"><i class="fa fa-3x fa-linkedin"></i></a>
                    </div>
                </div>
                <div class="contactIconContainer">
                    <div class="contactIcon">
                        <a target="_blank" href="https://github.com/steven-steven"><i class="fa fa-3x fa-github"></i></a>
                    </div>
                </div>
                <div class="contactIconContainer">
                    <div class="contactIcon">
                        <a target="_blank" href="mailto:s3@edu.uwaterloo.ca"><i class="fa fa-3x fa-envelope"></i></a>
                    </div>
                </div>
            </div>
            {{>footer}}
        </div>
    </div>

</body>
</html>
